@model CSDLGia_ASP.ViewModels.Manages.KeKhaiGia.VMKkGia
<div class="mb-3 font-weight-bold font-size-lg">
    <label style="color: blue; text-decoration-line: underline; font-weight: bold">Thông tin hồ sơ chi tiết</label>
</div>
<div class="card card-custom" style="border: 1px solid #60aee4;">
    <div class="card-header flex-wrap border-0 pt-6 pb-0">
        <div class="card-title">
            <h3 class="card-label">
            </h3>
        </div>
        <div class="card-toolbar">
            <button type="button" data-target="#Create_Modal" data-toggle="modal"
                    class="btn btn-primary font-weight-bolder" title="Create">
                <i class="la la-plus"></i>Thêm mới
            </button>
        </div>
    </div>
    <hr />
    <div class="card-body" id="frm_data">
        <table class="table table-striped table-bordered table-hover" id="datatable_4">
            <thead>
                <tr style="text-align:center">
                    <th width="2%">#</th>
                    <th width="2%">Stt</th>
                    <th>Tên dịch vụ cung ứng</th>
                    <th>Độ dài thời gian<br />chương trình hoặc<br />dịch vụ</th>
                    <th>Thời gian thực hiện<br />mức giá(Từ ngày)</th>
                    <th>Mức giá<br />kê khai</th>
                    <th>Hình thức kinh doanh</th>
                    <th width="9%">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null)
                {
                    @if (Model.KkGiaLuHanhCt != null)
                    {
                        @foreach (var item in Model.KkGiaLuHanhCt)
                        {
                            <tr>
                                <td style="text-align:center">@item.STTSapxep</td>
                                <td style="text-align:center">@item.STTHienthi</td>
                                <td class="active">@item.Tendvcu</td>
                                <td style="text-align:center">@item.Dodaitgian</td>
                                <td style="text-align:center">@Helpers.ConvertDateToStr(item.Thoigian)</td>
                                <td style="text-align:right">@Helpers.ConvertDbToStr(item.Giakk)</td>
                                <td>@item.Qccl</td>
                                <td>
                                    <button type="button" data-target="#Edit_Modal" data-toggle="modal"
                                onclick="SetEdit('@item.Id')" class="btn btn-sm btn-clean btn-icon" title="Chỉnh sửa">
                                        <i class="icon-lg la la-edit text-primary"></i>
                                    </button>
                                    <button type="button" data-target="#Delete_Modal" data-toggle="modal"
                                onclick="GetDelete('@item.Id')" class="btn btn-sm btn-clean btn-icon" title="Xóa">
                                        <i class="icon-lg la la-trash text-danger"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    }
                }
            </tbody>
        </table>
        <!--end: Datatable-->
    </div>
</div>
@await Html.PartialAsync("~/Views/Admin/Manages/KeKhaiGia/KkGiaLuHanh/DanhSach/Modal.cshtml")